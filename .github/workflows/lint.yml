name: Lint

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Run isort
        uses: actions/checkout@v3
        uses: isort/isort-action@v1
      - name: Run black
        uses: actions/checkout@v3
        uses: psf/black@stable